version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "migrations/"
    gen:
      go:
        emit_pointers_for_null_types: true
        package: "sqlitestore"
        out: "."
